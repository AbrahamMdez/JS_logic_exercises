'use strict'

/*
Mostrar todos los números impares que hay entre dos números introducidos por el usuario
*/


/* let number = parseInt(prompt('Numero', 0));
let number2 = parseInt(prompt('Numero', 0));

let print;

while(isNaN(number, number2)) {
    number = parseInt(prompt('Introduzca un numero', 0));
    number2 = parseInt(prompt('Introduzca un numero', 0));
};

if(number % 2 == 0 || number2 % 2 == 0) {
    document.write('No hay numeros impares');
} else {
    document.write(`<h1> El numero es impar ${number}`);
}; */